<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:DeXign.Theme">

    <Style x:Key="{x:Static local:ThemeKeyStore.Screen}" TargetType="{x:Type ContentControl}">
        <Setter Property="Background" Value="White"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ContentControl}">
                    <Grid>
                        <Grid Margin="-5,-25,-5,-40">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="5"/>
                                <ColumnDefinition/>
                                <ColumnDefinition Width="5"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="25"/>
                                <RowDefinition/>
                                <RowDefinition Height="40"/>
                            </Grid.RowDefinitions>
                            
                            <Rectangle Grid.ColumnSpan="3" Grid.RowSpan="3" RadiusX="5" RadiusY="5" Fill="#444444"/>
                            <Rectangle Grid.Column="1" Grid.Row="0" RadiusX="2" RadiusY="2" Height="4" Width="64" Fill="#47FFFFFF" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                            <Rectangle Grid.Column="1" Grid.Row="2" RadiusX="4" RadiusY="4" Height="17" Width="32" Fill="#47FFFFFF" Stroke="LightGray" StrokeThickness="1" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Grid>
                        
                        <Rectangle Fill="{TemplateBinding Background}" IsHitTestVisible="False"/>
                        <ContentPresenter/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>